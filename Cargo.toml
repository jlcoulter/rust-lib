[package]
name = "rust-lib-template"
version = "0.1.0"
edition = "2021"
description = "A publishable Rust library crate"
license = "MIT"

[dependencies]
thiserror = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"